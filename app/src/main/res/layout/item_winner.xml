<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="item"
            type="com.magma.miyyiyawmiyyi.android.model.Winner" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        app:cardUseCompatPadding="true"
        android:elevation="0dp"
        app:cardElevation="3dp"
        app:cardCornerRadius="@dimen/spacing_icon_padding">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/img_card"
                android:layout_width="@dimen/image_product_create_size"
                android:layout_height="@dimen/image_product_create_size"
                android:scaleType="fitCenter"
                android:padding="@dimen/spacing_default"
                tools:src="@drawable/gold_sack"
                image_res="@{item.imageCardRes}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"/>

            <TextView
                android:id="@+id/txt_title"
                style="@style/MaterialTextViewSmallStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginVertical="@dimen/spacing_default"
                android:text="@string/winner_100_usd_text"
                android:textColor="@color/grey_50"
                app:layout_constraintEnd_toStartOf="@id/lyt_winner"
                app:layout_constraintStart_toEndOf="@+id/img_card"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" />

            <androidx.appcompat.widget.LinearLayoutCompat
                android:id="@+id/lyt_winner"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/spacing_medium"
                android:layout_margin="@dimen/spacing_default"
                android:background="@drawable/bg_rounded_primary"
                app:layout_constraintWidth_percent="0.4"
                app:layout_constraintStart_toEndOf="@id/txt_title"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent">

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/img_icon"
                    android:layout_width="@dimen/default_icon_size"
                    android:layout_height="@dimen/default_icon_size"
                    android:scaleType="fitCenter"
                    android:layout_gravity="center"
                    android:layout_margin="@dimen/spacing_medium"
                    image_res="@{item.imageRes}"
                    tools:src="@drawable/ic_metro_trophy"/>

                <TextView
                    android:id="@+id/txt_winner"
                    style="@style/MaterialBoldTextStyle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:layout_gravity="center"
                    android:text="@{item.winnerName + `\n` + item.ticketNum}"
                    tools:text="@string/app_name"
                    android:textColor="@color/white" />

            </androidx.appcompat.widget.LinearLayoutCompat>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>